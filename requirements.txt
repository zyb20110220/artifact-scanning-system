# ============================================================
# 考古文物断代与鉴定系统 — 依赖清单
# 目标版本（阶段 1 任务 1.1：锁定版本并验证兼容性后填写 ✅）
# 安装顺序建议：先装 PyTorch（见下方注释），再装其余
# ============================================================

# ---------- 深度学习框架 ----------
# 【CPU 路线】PyTorch 由 Dockerfile 单独安装 CPU 版，不在本清单重复安装，避免版本覆盖：
#   pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cpu
# torch==2.4.1
# torchvision==0.19.1
transformers==4.43.3
peft==0.11.1
# bitsandbytes==0.43.1   # GPU only（4bit 量化需 CUDA），无 GPU 时删除
accelerate>=0.33.0
# xformers==0.0.27.post2  # GPU only，无 GPU 时不启用

# ---------- 视觉模型 ----------
# DINOv2: facebook/dinov2-base （通过 transformers 加载，无需额外包）
# CLIP:   openai/clip-vit-base-patch32 （transformers 内置）

# ---------- 多模态 LLM ----------
# 本地推理（Qwen2.5-VL）才需要 qwen-vl-utils
# 走云端 API 时无需 qwen-vl-utils，改用 openai 兼容 SDK
qwen-vl-utils>=0.0.7
# openai>=1.30.0   # 云端 API（无 GPU 路线推荐启用）

# ---------- 向量检索 ----------
faiss-cpu==1.7.4
# faiss-gpu==1.7.4  # Linux/WSL2 且有 NVIDIA GPU 时使用

# ---------- 知识图谱 ----------
neo4j>=5.19.0

# ---------- 数据与处理 ----------
pandas>=2.0.0
numpy>=1.26.0
Pillow>=10.0.0
requests>=2.31.0
pyarrow>=15.0.0   # Parquet
tqdm>=4.66.0
huggingface_hub>=0.23.0

# ---------- 前端 Demo ----------
gradio>=4.26.0

# ---------- 可视化 ----------
matplotlib>=3.8.0
plotly>=5.18.0
networkx>=3.2.0
pyvis>=0.3.2     # 证据链网络图

# ---------- 实验管理 ----------
tensorboard>=2.15.0
optuna>=3.5.0
# wandb>=0.16.0    # 可选：推荐免费层

# ---------- 数据版本控制 ----------
dvc>=3.0.0
