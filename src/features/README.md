# src/features — 视觉特征提取

**阶段 1 任务 1.3**

本模块用于：

- 加载 DINOv2 (`facebook/dinov2-base`) 提取 768 维视觉特征
- 加载 CLIP (`openai/clip-vit-base-patch32`) 提取 512 维特征
- 批量推理，结果保存为 Parquet

> 📝 该目录当前为空，等待阶段 1 逐步编写。每完成一个脚本，请在 `PROGRESS.md` 对应任务打勾。
